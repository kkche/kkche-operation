安装compass和grunt：
sudo gem update --system
sudo gem install compass
sudo npm install -g grunt-cli

安装yeoman：
sudo npm install -g yo

安装bower：
npm install -g bower

安装前端依赖的js包（建议穿墙）：
bower install

安装依赖的npm包：
npm install
